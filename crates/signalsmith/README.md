# signalsmith

This crate contains a partial Rust port of
[signalsmith-stretch](https://github.com/Signalsmith-Audio/signalsmith-stretch).

The code in `src/port` is intended to be a faithful port of the upstream
implementation. It stays structurally close to the original so it is easier to
compare, audit, and verify against upstream behavior.
